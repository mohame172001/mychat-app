# Project Changelog

Pending Phase 2R fresh-comment anchor state entry: 2026-05-31, `(pending)` / `fix(admin): prevent stale fresh-comment anchor filters`. Webhook Verification no longer displays a real old timestamp as the fresh-comment anchor placeholder or copies unsent UI state as an active filter. The anchor is empty by default, validates only `YYYY-MM-DDTHH:mm:ssZ`, sends `after_utc` only when the visible input contains a valid value, clears on username change, and has an explicit Clear anchor button that reloads without a cutoff. Backend `after_utc` parsing is request-scoped: empty/whitespace/`null` means no cutoff, invalid non-empty values return a clear validation error, and cutoff-dependent fresh-comment signals are omitted unless a valid cutoff is applied. Polling remains disabled by default; no send behavior, Billing, HMAC, dedupe, Phase 2D cooldown, rate limits, quick reply copy, DM automation, Dashboard routes, or username-specific behavior changed.

Pending Phase 2Q external-comment visibility classifier entry: 2026-05-31, `(pending)` / `fix(instagram): classify external commenter visibility in webhook tests`. After live evidence proved self-comments from the connected IG owner arrive through `source=webhook` and skip correctly as `bot_own_reply`, the remaining gap narrowed to external tester comments missing from Webhook Verification. The existing Direct Graph comment check now uses the active `instagram_accounts` row token to query `/{media-id}/comments`, applies the same `after_utc` fresh-comment anchor, and cross-checks matching Graph comment partial ids against flight-recorder rows. It classifies outcomes as `external_comment_visible_in_graph`, `external_comment_visible_in_graph_but_no_webhook_event`, `external_comment_not_visible_in_graph`, `external_comment_arrived_under_different_media`, `external_comment_filtered_before_logging`, or `graph_comments_read_failed`. It also fixes the Graph timestamp parser call from `parse_graph_datetime` to `_parse_graph_datetime`, so `matched_by_time_window` is meaningful. Responses remain admin-only/read-only and expose only partial ids, created time, match booleans, counts, bounded redacted Graph errors, and no raw text/tokens/full ids. The UI reuses the existing Direct Graph comment check, forwards `after_utc`, and does not add a new form. Polling remains disabled; no automation send behavior, Billing, HMAC, dedupe, Phase 2D cooldown, rate limits, quick reply copy, Dashboard routes, DM automation, or username-specific behavior changed. Tests: Graph-visible/no-webhook, Graph-not-visible, visible-and-webhook-detected, different-media, filtered-before-logging, and UI structural coverage.

Pending Phase 2P webhook-verification cutoff/classifier entry: 2026-05-31, `(pending)` / `fix(instagram): apply fresh-comment cutoff and classify bot-own webhook skips`. Live webhook delivery for `muhammad_gehad` produced a correct self-comment skip (`automation_skipped`, `skip_reason=bot_own_reply`), but the Webhook Verification flow classifier labeled it `webhook_in_flight`, and copied JSON did not include the active fresh-comment cutoff even when the UI field was set. The admin UI Copy JSON wrapper now includes `after_utc`, `after_local`, and `after_time_utc_effective`; the backend endpoint continues to apply `after_utc` to the fresh-comment signal and tests now assert it. `_compute_webhook_flow_verdicts` now classifies webhook bot-own skips as terminal `webhook_skipped_bot_own_reply` with commenter/skip/bot-own/terminal metadata. `_compute_fresh_comment_signal` now separates `only_bot_own_comment_seen_after_cutoff` from `fresh_external_comment_no_webhook_signal_after_comment_time`, exposing safe partial comment/commenter identifiers so operators can tell whether only a self-comment arrived after the cutoff or the external tester comment is still missing. Webhook Verification displays commenter id, bot-own, skip reason, and terminal status in the event/verdict tables. Polling remains disabled; no automation send behavior, Billing, HMAC, dedupe, Phase 2D cooldown, rate limits, quick reply copy, Dashboard routes, DM automation, or username-specific behavior changed. Tests: fresh-cutoff endpoint echo/application, bot-own terminal flow verdict, fresh-comment signal bot-own/missing-external labels, and UI structural coverage.

Pending Phase 2O repair/readback entry: 2026-05-31, `(pending)` / `fix(instagram): expose and fix webhook subscription graph readback failure`. The admin Repair comment-webhooks action still uses the active `instagram_accounts` row token and IG business id for both Graph `POST /{ig-user-id}/subscribed_apps` and the immediate Graph `GET /{ig-user-id}/subscribed_apps` readback, but it no longer collapses readback failures into a generic `repair_graph_readback_failed`. The helper now parses the readback response safely, records subscribed fields only when the JSON shape is valid, and classifies failures as precise redacted reasons (`graph_readback_permission_denied`, `graph_readback_unauthorized`, `graph_readback_wrong_object_id`, `graph_readback_http_failed`, `graph_readback_timeout`, `graph_readback_parse_error`, `graph_readback_missing_data_array`, `graph_readback_unexpected_shape`, `graph_readback_empty_response`). Certification persists the readback reason/HTTP status/response keys/Graph error code+subcode+message in redacted fields and uses blocker `repair_graph_readback_failed:<reason>` when subscribe POST was accepted but readback failed. The repair API response and Webhook Verification UI show `readback failed: <reason>` plus safe response keys/status so operators can distinguish wrong object, permissions, shape, timeout, and missing-field cases. It does not mark `comment_permission_not_granted` unless the correct active token proves that permission is absent. Polling remains disabled/default webhook-first; no Billing, HMAC, dedupe, Phase 2D opening-DM cooldown, rate-limit, quick-reply copy, DM automation, Dashboard/frontend routes, or username-specific behavior changed. Tests: certification gate expanded with precise readback cases and direct helper coverage; Webhook Verification structural test updated for readback copy.

Pending Phase 2C-C UI clarity entry: 2026-05-28, `(pending)` / `chore(admin): clarify webhook verification freshness and filters`. Builds on the Webhook Verification tab. (a) Default `username` filter is now `muhammad_gehad` (operator UI default only — input is editable, no automation logic branches on the value; static-source test enforces). (b) Active filters + timestamps are displayed above the events table: applied filter values from the backend response, last-refreshed-at (browser local), query window start→end (local with UTC tooltip), server-now (local with UTC tooltip), browser timezone. (c) Quick range chips: 10 / 30 / 60 / 120 min — each triggers an immediate reload. (d) `since_minutes` input rendered with shared `WV_INPUT_CLASS` so height / border-radius / padding / font match the other filter inputs; `inputMode="numeric"` and `parseInt`-based controlled value so it never displays a stray non-numeric character. (e) Warning banner when `username` is empty: "No username filter selected — results may include unrelated or older events." (f) Reload-after-test helper note next to the quick-range chips. (g) Events table: source rendered as a badge (webhook=sky / polling=amber), stage rendered as badge for `automation_success`/`webhook_comment_detected`, skip_reason rendered as badge for `already_replied_success` / `bot_own_reply` / `historical` / `*_failed`. `created_at` shown in local time with raw UTC value in tooltip. (h) View toggles: Show webhook only / Show successes only / Hide re-scans + already_replied_success — client-side filter; shows `visible / total` count. (i) Copy JSON now wraps the backend response with client-side metadata: `copied_at_local`, `browser_timezone`, `active_filters`, `ui_view_filters`, `backend_response`. Backend response itself unchanged in shape. (j) Backend endpoint adds four read-only diagnostic top-level fields: `server_now_utc`, `window_start_utc`, `window_end_utc`, `applied_filters`. No automation logic, dedupe, HMAC, rate-limit, billing, quick-reply copy, polling pipeline, or send-behavior change. Tests: new `WebhookVerification.test.js` (11 structural assertions including no-username-branching); `AdminConsole.reliability.test.js` 6 passed; `App.routes.test.js` 2 passed; backend `test_phase2c_c_webhook_verification.py` 11 passed (+1); backend full 748 passed (+1); frontend production build passed.

Pending Phase 2C-C UI entry: 2026-05-28, `(pending)` / `chore(admin): add webhook verification diagnostics UI`. Adds a new `WebhookVerificationTab` to `AdminConsole.jsx` so the operator can call the existing `/api/admin/instagram/webhook-verification` endpoint from the browser using the existing authenticated frontend API client — no shell admin JWT needed. Tab is gated by the same `canViewUsers` permission as the other diagnostic tabs (Stop Point / Rule Coverage / Webhook Health / Flight Recorder). Renders the summary counters (webhook_comments_success_count, polling_success_count, webhook_comment_events_seen_count, webhook_comments_unmapped_count, phase2c_b_fallback_used_count, alias_self_heal_count, latest_webhook/polling timestamps) plus an event table with the safe partial-identifier fields the backend already redacts. Filter inputs: `username` (blank = all accounts), `since_minutes` (default 10), `comment_id_partial`, `media_id_partial`. "Copy JSON" button writes the raw response to the clipboard so it can be pasted into a chat. Diagnostic-only — calls a read-only endpoint, surfaces only the fields the backend chose to expose, no automation logic touched. No backend changes; no public-user route; no `/app/admin/instagram-diagnostics` reintroduction (`App.routes.test.js` continues to enforce its absence). Tests: frontend AdminConsole reliability suite 6 passed; App routes suite 2 passed; frontend production build passed.

Pending Phase 2C-C webhook-verification endpoint entry: 2026-05-28, `(pending)` / `chore(instagram): add safe webhook verification diagnostics`. Adds `GET /api/admin/instagram/webhook-verification` so the operator can verify in one HTTP call whether Meta's comment webhooks arrived, mapped to an `instagram_accounts` row, parsed correctly, and called `_handle_new_comment(source='webhook')` for a given window. Admin-gated via the existing `_require_admin_permission(PERM_USERS_VIEW)`. Read-only — no DB writes, no token reads (defensive test enforces no send/dedupe/insert/update call shapes in the endpoint body). Filters: `username`, `since_minutes` (clamped 1-1440), `comment_id_partial`, `media_id_partial`, `limit` (clamped 1-1000). Summary counters distinguish webhook vs polling success paths and count Phase 2C-B's media-owner fallback + alias self-heal usage. Per-event payload exposes only the allow-listed safe partial-identifier fields the flight recorder already stores; tokens, full payloads, raw text, and authorization headers are excluded by construction. `connected_account_samples` is capped at 10 entries and keys are restricted to the four documented ones. No sending behavior, dedupe semantics, HMAC verification, rate limits, quick-reply copy, polling pipeline, Billing, or Dashboard/frontend changed. Account-agnostic — no username branching (static-source test enforces). Tests: `test_phase2c_c_webhook_verification.py` 10 new tests covering auth, redaction, filters, summary correctness, since-minutes clamping/window, read-only contract, no-username-branching. Full backend 747 (+10).

Pending Phase 2C-B webhook media-owner resolver entry: 2026-05-28, `(pending)` / `fix(instagram): resolve unmapped comment webhooks by media owner`. The existing primary `_find_user_doc_for_instagram_account_id(entry.id)` resolver remains first and unchanged for successful mappings. When a comment webhook is unresolved by `entry.id`, the backend extracts the comment media id from supported payload aliases, performs a bounded account-agnostic probe across active/valid connected Instagram accounts with tokens, and resolves only when exactly one account can read that media. Zero matches fail closed; multiple matches fail closed as `ambiguous_media_owner_resolution`; invalid/expired tokens are skipped without crashing. Successful fallback calls `_handle_new_comment(..., source='webhook')` and self-heals the incoming `entry.id` into `instagram_accounts.webhookEntryIdAliases` with idempotent `$addToSet`, so later webhooks route through the primary alias path without probing. Failure diagnostics include sanitized partial identifiers and bounded connected-account identity samples only; no tokens, secrets, full ids, full payloads, Billing, HMAC, dedupe semantics, rate limits, quick reply copy, Dashboard/frontend, polling fallback, or username-specific behavior changed. Tests: `py_compile backend/server.py`, `test_phase2c_b_webhook_media_owner.py` 14 passed, `test_multi_account_automation_routing.py` 157 passed, `test_phase2_media_catalog_round_robin.py` 7 passed, full backend suite 737 passed. Not known-good until live webhook fast-reply verification passes.

Pending Phase 2D opening-DM cooldown entry: 2026-05-27, `(pending)` / `fix(instagram): prevent repeated opening DMs for same commenter`. Public replies remain per physical comment, but opening DMs are now controlled by `COMMENT_DM_OPENING_DEDUPE_WINDOW_SECONDS` (default 86400, clamped 3600-604800) using the existing key shape `user_id + instagram_account_id + automation_id/rule_id + media_id + commenter_id`. A shorter `COMMENT_DM_COMPLETED_FLOW_REOPEN_TTL_SECONDS` no longer reopens opening-DM eligibility inside the 24-hour window. Repeated same-commenter/same-media/same-rule/account comments inside the window skip only the opening DM with `opening_dm_already_sent_for_commenter_media`; different commenters and same commenter on different media remain eligible according to existing policy. Webhook and polling paths have parity. No Billing, HMAC, exact-comment dedupe weakening, rate-limit, quick reply copy, Dashboard/frontend, or username-specific behavior changed. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 157 passed (+5), `test_phase2_media_catalog_round_robin.py` 7 passed, full backend suite 723 passed (+5, excluding incomplete stashed Phase 2C-B WIP test).

Pending Phase 2C-A polling-noise reduction entry: 2026-05-26, `(pending)` / `fix(instagram): reduce polling noise and summarize scans`. Production is on Phase 2B `237e549`, which prevents delayed replies to stale polling comments, but the poller still fed old polling rows into `_handle_new_comment` and emitted full noisy traces (`poller_comment_seen`, `dedupe_checked`, rule loading, rule evaluation, historical skips) for old comments every tick. `_poll_user_comments` now prefilters polling-only comments with provider timestamps when the matching rule would skip them as `historical_before_rule_activation` or `stale_polling_comment` and `process_existing_unreplied_comments` is false, then counts them in one compact `polling_scan_summary` instead of entering the full automation pipeline. Fresh polling comments, webhook comments, missing-timestamp polling comments, and explicit selected-post catch-up still use the full trace. Stop Point now reads `polling_scan_summary` and reports `only_historical_or_stale_comments_seen` / `no_fresh_comment_seen_in_poll` instead of misleading `rule_not_matched` when no fresh comment was scanned. No Billing, HMAC, dedupe semantics, rate limits, quick reply copy, Dashboard/frontend, webhook send behavior, or username-specific behavior changed. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 152 passed (+4), `test_phase2_media_catalog_round_robin.py` 7 passed, full backend suite 718 passed (+4).

Pending Phase 2B stale polling guard entry: 2026-05-26, `(pending)` / `fix(instagram): guard stale polling comments`. Production is on Phase 2 `ed3d842`, which correctly adds media catalog + round-robin coverage for any-post rules. Live evidence showed round-robin can also first-see old comments on old media after rule activation and process them as delayed live interactions. `_handle_new_comment` now keeps the existing pre-activation historical skip unchanged, but adds a polling-only guard after activation: if `source='polling'`, `process_existing_unreplied_comments` is false, Graph supplied a comment timestamp, and comment age exceeds `IG_POLL_FRESH_COMMENT_WINDOW_SECONDS` (default 3600, clamped 60-21600), the comment is skipped as `stale_polling_comment` before public reply/DM. Explicit selected-post catch-up remains allowed; webhook events are not blocked by this guard; exact-comment dedupe, opening-flow dedupe, HMAC, rate limits, quick reply copy, dashboard, and account scoping are unchanged. Tests added for stale polling skip, webhook not blocked, selected-post catch-up still allowed, and no username-specific branches. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 148 passed, `test_phase2_media_catalog_round_robin.py` 7 passed, backend full 714 passed. Local, deploy required.

Important commits only. Keep this table updated after every meaningful commit.

Pending Phase 2 media-catalog + round-robin polling entry: 2026-05-26, `(pending)` / `feat(instagram): media catalog + round-robin polling for full any-post coverage`. Required to make `comment:any / post_scope=any` rules actually cover every post on the account, not just the latest 25. New `instagram_media_catalog` collection (per-account, unique on `(instagramAccountDbId, media_id)`, sort index on `media_timestamp`) populated by paginated `/me/media` via `_sync_instagram_media_catalog` on a 10-minute TTL per account (env-tunable, clamped 60s-24h). `_collect_target_media_ids` now composes three slices for any-post rules: (a) recent N from live `/me/media` (env `IG_POLL_RECENT_MEDIA_LIMIT`, default 25, clamped 1-100), (b) pinned `selected_media_id` from post-specific rules (always included), (c) round-robin batch from the persisted catalog (env `IG_POLL_ROUND_ROBIN_BATCH`, default 25, clamped 0-100; set to 0 disables). A per-account `pollingRoundRobinCursor` on `instagram_accounts` advances each tick with wrap-around so the entire catalog is eventually covered on a bounded schedule without scanning every post every 15s. `_poll_user_comments` per-tick scan cap raised from `media_ids[:25]` to recent+round-robin+headroom (~75). `_fetch_recent_media_ids` hard cap raised from 25 to 100 so the env var can scale the recent slice. New `poller_account_scan_started.extra` fields: `total_known_media_count`, `recent_media_limit`, `round_robin_batch_size`, `round_robin_cursor_position`. Catalog sync runs best-effort (try/except wrapped); polling continues with whatever the catalog currently holds. Webhook path unchanged — any comment that arrives via webhook still enters `_handle_new_comment` directly regardless of catalog state. Historical-comment protection preserved: old comments without `process_existing_unreplied_comments` still skip. Account-agnostic — no username branching, no per-account pipeline. `IG_POLL_REPLY_CAP_PER_RUN` still bounds write actions. Tests: `test_phase2_media_catalog_round_robin.py` 7 new tests; `test_multi_account_automation_routing.py` updated to tolerate the extra Graph calls catalog sync makes; full backend 710 passed (+7). Local, deploy required. No Billing, HMAC, dedupe, rate-limit, send-behavior, dashboard, or quick-reply copy change.

Pending Phase 1 SaaS hardening entry: 2026-05-26, `(pending)` / `fix(saas): phase 1 multi-tenant + storage hardening`. Required before onboarding real users. (a) Single-tenant webhook fallback at `_process_webhook` is now disabled by default in production behind `INSTAGRAM_SINGLE_TENANT_FALLBACK=1`; remains enabled outside production for local/preview parity. Protects the first SaaS customer from having any unmapped webhook silently attributed to their account. (b) Story Reply automation loading at the `story_insights` branch now uses `_account_scoped_query(user_id, ig_account_id)` so a sibling-account rule under the same owner cannot fire on a different account's story webhook. (c) Three new TTL indexes on transient collections: `ttl_webhook_dlq_terminal_at` (30d after status transitions to `replayed`/`permanently_failed`; active `pending_retry` rows have `terminal_at` unset and are never pruned), `ttl_comment_dm_sessions_expires_at` (30d past natural `expiresAt`; active pending sessions have `expiresAt` in the future and are never pruned), `ttl_link_click_events_clicked_at` (90d; dashboard already limits reads to 5000 most-recent clicks per user so retention does not break aggregation). All three are env-tunable, clamped to safe windows, and use the same collMod-on-conflict fallback pattern as the prior `instagram_automation_events` TTL. (d) Stop Point reporting: success-wins-over-dedupe override so an `automation_success` event for a comment surfaces over a later polling re-scan `automation_skipped(already_replied_success)` for the same comment; genuine failures still win over success. Added literal `'historical'` skip_reason to the `no_fresh_comment_seen_in_poll` override set so polling-only accounts whose latest visible event is a months-old historical re-scan no longer show `exact_stop_reason=historical`. Reporting-only — no sending, dedupe, HMAC, rate-limit, billing, dashboard, or quick-reply copy change. No core-business-data collection is TTL'd (defensive test enforces this). Tests: `test_phase1_saas_hardening.py` 13 new tests passed; `test_multi_account_automation_routing.py` continues to pass; backend full 703 passed (+13). Local, deploy required.

Pending storage-retention entry: 2026-05-26, `(pending)` / `fix(infra): cap instagram_automation_events with 7-day TTL`. After Atlas's 512MB free-tier quota filled with ~465MB of `instagram_automation_events` (blocking ALL writes including automations, dashboard, sessions, OAuth), add a TTL index `ttl_instagram_automation_events_created` on `{created_at: 1}` with `expireAfterSeconds=604800` (7-day default). Index creation lives inside the existing non-blocking `_index_bootstrap` task — no Railway healthcheck impact. Window is tunable via `IG_AUTOMATION_EVENTS_TTL_SECONDS` env var, clamped to `[86400, 7776000]` so a typo cannot empty the collection or silently re-create the overflow. On `IndexOptionsConflict` (existing TTL with different window), a `collMod` fallback updates the window in place. Diagnostic events older than the window are pruned by Atlas's background TTL monitor (60s cadence). Admin Stop Point / Flight Recorder / Webhook Health continue to work — Stop Point's classification window is ~100-200 events / 7 days, well inside the retention window. No automation-execution, dedupe, HMAC, send, rate-limit, billing, dashboard, or quick-reply change. Event-noise reduction deliberately deferred — every event class is currently load-bearing for at least one Stop Point branch. Tests: `test_index_bootstrap_creates_instagram_automation_events_ttl_index`, `test_index_bootstrap_clamps_ttl_within_safe_bounds`, `test_index_bootstrap_ttl_failure_does_not_crash_other_indexes`; `test_startup_bootstrap.py` 6 passed (+3); backend full 690 passed (+3). Local, deploy required.

Pending Instagram polling-coverage entry: 2026-05-26, `(pending)` / `fix(instagram): widen polling target media + correct Stop Point decision matrix`. (a) Polling target-media collection for needs_any (general / post_scope=any) rules now requests the function's hard cap (25) recent posts instead of 10, and `_poll_user_comments` widens its per-tick scan from `media_ids[:10]` to `media_ids[:25]` so a fresh comment on the 11th+ most recent eligible post is actually discovered. Actual write actions remain bounded by `IG_POLL_REPLY_CAP_PER_RUN`. (b) `poller_account_scan_started` flight-recorder event now carries `target_media_count`, `target_media_partials`, and `rules_count`. (c) `apply_activation_cutoff` now records a `historical_catchup_allowed` flight event whenever a post-specific rule with `process_existing_unreplied_comments=true` on the matching selected media legitimately processes a pre-activation comment. (d) Stop Point summarizer implements the full decision matrix: `exact_stop_reason=no_fresh_comment_seen_in_poll` when polling saw no fresh external comment and the latest visible row is either a bot-own reply or an already-processed re-scan (overrides the generic `rule_not_matched` / `already_replied_success` / `historical_before_rule_activation` collapse); `historical_before_rule_activation` preserved for truly fresh-but-pre-activation comments under `process_existing=false`; `process_existing_unreplied_comment_processed` emitted when the historical-catchup path produced an `automation_success`; `selected_media_no_match` / `rule_not_matched` preserved for genuine no-match cases. (e) `historical_catchup_allowed` exposed as a top-level summary flag. (f) Recommendation text updated for the new labels. Account-agnostic; no username special-casing; no Billing, HMAC, dedupe-provider-proof, rate-limit, frontend diagnostics route, webhook execution, dashboard, account-scope, or quick-reply-copy change. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 144 passed (+5), `test_historical_unreplied.py` + `test_historical_specific_post.py` 15 passed, `test_webhook_timestamp_fallback.py` 7 passed, backend full 687 passed (+5). Local, deploy required.

Pending Instagram quick-reply parity entry: 2026-05-25, `(pending)` / `fix(instagram): unify quick reply behavior across accounts`. Outgoing Instagram DMs no longer auto-append visible browser/laptop fallback copy to quick-reply messages. The creator-authored message body is sent unchanged for every linked Instagram account, quick reply buttons and payloads remain unchanged, and internal typed fallback continuation remains scoped to a valid pending comment-DM session. This supersedes the earlier visible fallback-copy behavior from the dedupe-per-post patch. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 139 passed, backend full 682 passed. Local, deploy required. No Billing, HMAC, dedupe removal, rate-limit removal, frontend diagnostics route, webhook/polling execution, dashboard, or account-scoping change.

Pending Instagram polling freshness entry: 2026-05-25, `(pending)` / `fix(instagram): prioritize fresh polling comments over old rescans`. The poller now asks Graph for `order=reverse_chronological` on media comments and still locally sorts the bounded returned slice so external comments with newest provider timestamps run before bot-owned replies and old already-processed rescans. The manual catch-up path uses the same ordering. Stop Point now exposes fresh-comment fields and reports “No new Instagram comment was returned by polling” when the latest visible row is only an old processed re-scan. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 138 passed, backend full 681 passed. Local, deploy required. No Billing, Dashboard UI, HMAC, dedupe removal, rate-limit removal, frontend diagnostics route, or account-scoping change.

Dashboard UX entry: 2026-05-25, `c084311` / `fix(dashboard): simplify overview and fix range chart labels`. Dashboard now keeps only the four primary KPI cards prominent, hides secondary stats behind a collapsed More stats toggle, uses sparse range-aware chart labels, keeps previous range data visible while refreshing, seeds the default `7d` dashboard cache through bootstrap/warmup, and caps Top Automations at three rows. Tests: frontend 187 passed; frontend production build passed. Deployed. No backend, Instagram automation, Billing, HMAC, dedupe, webhook/polling, or rate-limit change.

Pending Instagram historical-cutoff entry: 2026-05-25, `(pending)` / `fix(instagram): prevent fresh comments from being marked historical`. Polling comments with no Graph timestamp now use the safe first-seen timestamp instead of being treated as unprovable/historical. Real Graph timestamps remain authoritative, so truly old pre-activation comments still skip unless existing settings allow processing old comments. Existing comment docs with stale `historical_before_rule_activation` skips are reprocessed only when the current payload proves the comment is at/after the stored activation cutoff. Stop Point now surfaces latest external comment partial id/media id/timestamp/timestamp source/activation cutoff details so historical skips do not collapse into generic `rule_not_matched`. Tests: `py_compile backend/server.py`, `test_multi_account_automation_routing.py` 135 passed, `test_webhook_timestamp_fallback.py` 7 passed, backend full 678 passed. Local, deploy required. No Billing, HMAC, dedupe removal, rate-limit removal, dashboard, frontend diagnostics route, or account-scoping change.

| Date | Commit | Area | Summary | Behavior changed? | Tests | Deploy status |
|---|---|---|---|---|---|---|
| 2026-05-25 | `9b2af1b` | Dashboard polish — range labels + stat presentation | Frontend-only follow-up to the previous timestamp formatting commit. (a) Chart title is now range-aware: 24h → "Performance — Last 24 hours", 7d → "Performance — Last 7 days", 30d → "Performance — Last 30 days", all → "Performance — All time" (replaces the static "Weekly Performance" that read wrong for non-7d ranges). (b) Tooltip + axis labels now FORCE the app UI language (`en` / `ar`) instead of falling through to the browser system locale — fixes "24 22:00 مايو" for English-UI users on Arabic-locale browsers. (c) More stats toggle is now a chip-style button with ChevronDown/ChevronUp icons, hover/focus styling, and i18n labels ("More stats" / "Less stats" / "مزيد من الإحصاءات" / "إخفاء الإحصاءات"); previous plain text wasn't obviously clickable. (d) Total Contacts subtitle clarified to "All-time · Active account" (was "Active Instagram account") so it can't be mistaken for a range-scoped metric next to the range-scoped cards. (e) Top Automations: active automations sorted first, paused/draft rows visually de-emphasized (opacity-70) and a subtitle "Active first · by sends" so the main dashboard stays focused on actionable rules. No backend, automation, dedupe, HMAC, rate-limit, webhook, or polling change. | No backend behavior change | `frontend dateTime suite` 14 passed (+3); `Dashboard.performance.test.js` 11 passed (+4); full frontend 205 passed (+7); frontend production build passed; App.routes.test.js focused 2 passed | Local, deploy required |
| 2026-05-25 | `4912756` | Frontend UI formatting | Centralized date/time formatting helper (`frontend/src/lib/dateTime.js`) for visible UI timestamps. `formatChartTooltipTitle(value, range)` and `formatChartAxisLabel(value, range)` keep dashboard chart text human-readable: 24h → "Today, 21:00" / "May 24, 21:00"; 7d → "Thu, May 21"; 30d → "May 21"; all → "May 2026". Dashboard tooltip and x-axis route through the helper instead of concatenating `day,date` (which produced "21h, 2026-05-24T21:00:00"). Admin Webhook Health top-level + per-account timestamps and Flight Recorder event-row `created_at` now render via `formatDateTime` / `formatCompactDateTime` (e.g. "Today, 14:40" or "May 25, 14:40:07") with the raw ISO retained on `title` hover and in Copy JSON. Handles null/undefined/invalid input as "-". Uses browser local timezone via `Intl.DateTimeFormat`. Frontend only — no backend, automation, dedupe, HMAC, rate-limit, webhook, or polling change. | No backend behavior change | `frontend dateTime suite` 11 passed; full frontend 198 passed (+14); frontend production build passed; App.routes.test.js focused 2 passed | Local, deploy required |
| 2026-05-25 | (pending) | Instagram dedupe + quick reply fallback | Comment event dedupe now refuses to let an older processed comment document from a different `media_id` block a fresh comment on another post. Business opening-flow dedupe remains account + rule + media + commenter scoped, so same commenter/same post/same rule and exact duplicate comments still skip. Opening DMs with quick replies now append an automatic browser/laptop typed fallback instruction derived from the quick reply title, e.g. Arabic `لو الزر مش ظاهر عندك، ابعت كلمة: ابعت` or English `If the button is not visible, reply with: send`. Existing automations benefit at send time; creators do not need to edit messages manually. Mobile quick replies remain unchanged. | Yes (dedupe scoping edge case + opening DM text sent to users) | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 131 passed; backend full 674 passed | Local, deploy required |
| 2026-05-25 | (pending) | Dashboard range filter + visual redesign | New `?range=24h|7d|30d|all` query param on `/api/dashboard/summary`. Per-range chart buckets (24 hourly / 7 daily / 30 daily / 12 monthly). Window-scoped counters for messagesSent / conversionRate / commentsProcessed / publicRepliesSent / dmsSent / linkClicks. Cache key + `dashboard_summaries` doc key extended with `range`. Strict multi-account scoping: `_dashboard_scoped_docs` no longer broad-falls-back to `user_id` when the workspace has ≥2 active linked accounts and no active context — fixes the Total Contacts cross-account leak that combined both linked accounts on the active-account dashboard. Frontend: compact range pill selector in header (persisted to localStorage), tighter 4-card KPI grid, lighter secondary KPI row, simplified chart + Top Automations panels. No backend automation, rule-matching, webhook, polling, dedupe, HMAC, or rate-limit change. | Yes (dashboard scoping/filter + UI redesign) | `py_compile backend/server.py`; `test_dashboard_summary.py` 25 passed (+7); backend full 668 passed (+7); frontend 184 passed; frontend production build passed; App.routes.test.js focused 2 passed | Local, deploy required |
| 2026-05-25 | `ef0e132` | Instagram comment dedupe classification | `_handle_new_comment` catch-all `else` block (server.py:17426) used to always return `automation_skipped` with `skip_reason='unknown_state'` whenever it could not classify the existing comment doc into a known processed state (`already_replied_success`, `historical`, `partial_success`, `dm_failed`, `pending`). For an incomplete / seen-only existing doc (no `reply_status=success`, no `dm_status=success`, no `replied` proof, no canonical skip reason) this incorrectly blocked rule loading and silently swallowed legitimate polling comments. Now: only the known-classified branches keep their skip; the unclassifiable case sets `retry_existing=True` and falls through to rule loading. Downstream `opening_dedupe_key` and provider-proof checks in the send path still independently prevent duplicate sends. New flight-recorder event `existing_comment_unknown_state_reprocess` records the decision. Account-agnostic; no rule-matching, send, dedupe, HMAC, or rate-limit change. | Yes (existing-comment dedupe classification path) | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 125 passed (+3); backend full 661 passed (+3) | Local, deploy required |
| 2026-05-25 | `dae71eb` | Admin diagnostic summaries | Fix four admin-panel summary inconsistencies (all reporting/aggregation, no automation change). (a) `summarize_account_automation_stop_point`: `account_resolved` now accepts any polling-stage event as proof of resolution since polling never writes `account_resolution_success`; `polling_scanned_account` broadens to also accept `poller_media_scan_started` / `poller_comment_seen`. (b) The literal `'rule_not_matched'` fallback is replaced with `early_exit_before_rule_loading` when an `automation_skipped` fired without a `skip_reason` AND `rule_loading_finished` is absent. (c) `unknown_state` / `comment_processed_unknown_state` now promotes `extra.classified_reason` so the displayed reason is actionable. (d) Summary gains `classified_reason`, `is_latest_event_rescan_of_processed`, `is_latest_event_historical`. (e) `multi-account-health` per-account adds `last_comment_webhook_event_time`, `last_messaging_webhook_event_time`, `last_account_resolution_failed_at`, and `webhook_delivery_status` ∈ {comment_webhooks_received, comment_webhooks_observed_globally_not_mapped, polling_fallback_only}. (f) Frontend: Flight Recorder classifier surfaces the concrete skip reason instead of `silent_early_exit_possible`; Webhook Health renders a delivery-status badge; Stop Point card displays `classified_reason` and state badges. No automation, rule-matching, webhook/polling logic, dedupe, HMAC, or rate-limit change. | Yes (admin summary aggregation + UI clarity) | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 122 passed (+8); backend full 658 passed (+8); frontend 184 passed; frontend production build passed; App.routes.test.js focused 2 passed | Local, deploy required |
| 2026-05-25 | `4ea7bef` | Dashboard metrics | Conversion Rate now uses current-month unique contacts (commenters this month ∪ link-click users this month, minus the workspace's own IG account id) as the denominator instead of all-time `totalContacts`, so the numerator and denominator share the same time window. Top Automations ordered by `sent` desc with active-first and `created` desc as tie-breakers — was `created` desc only. Frontend cards gain "This month" / "Active account" subtitles. New secondary KPI row (Comments Processed, Public Replies, Opening DMs, Link Clicks, Connected Accounts) consumes existing summary payload fields — no backend payload change. Dashboard / statistics only — no Instagram automation, rule-matching, webhook, polling, dedupe, HMAC, or rate-limit change. | Yes (dashboard metric formula + UI labels) | `py_compile backend/server.py`; `test_dashboard_summary.py` 18 passed (+5); backend full 650 passed (+5); frontend 184 passed; frontend production build passed; App.routes.test.js focused 2 passed | Local, deploy required |
| 2026-05-25 | `948a996` | Instagram polling target | `_collect_target_media_ids` now uses the canonical-trigger / canonical-post_scope helpers (the same ones 54fb527 introduced for rule classification) so a legacy general rule whose top-level `trigger` is `Manual` / empty but whose `nodes[].data.trigger='comment:any'` or `post_scope='any'` correctly sets `needs_any=True`, calls `_fetch_recent_media_ids`, and brings the user's new post into the polling target list. Account-agnostic; rule-matching, dedupe, send, and rate-limit paths are unchanged. Post-specific rules remain strictly scoped to their selected media. | Yes (polling target collection only — no automation execution change) | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 114 passed (+4); backend full 645 passed (+4) | Deployed; **live-verified** via a fresh comment from a completely new external Instagram account on a new post on muhammad_gehad — automation reply + opening DM succeeded via polling fallback; mogehad17 webhook path unchanged. Known-good for multi-account general automation + polling target collection. |
| 2026-05-24 | `d4dae1a` | Admin support reporting | Every silent dedupe early-return in `_handle_new_comment` now records an `automation_skipped` flight-recorder event with its existing reason string before returning (`comment_already_pending_queue`, `already_replied_success`, `comment_already_partial_success` (×2 paths), `comment_already_dm_failed`, `public_reply_required_recovery`, plus the generic catch-all using the existing `return_reason`). Stop Point will now show the real duplicate/already-processed reason instead of the misleading literal `rule_not_matched` when the operator re-tests by repeatedly commenting from the same external account on the same post. No automation execution change, no extra reply/DM, no rule-matching change, no dedupe weakening, no rate-limit/pacing change. | No (visibility only) | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 110 passed (+4); backend full 641 passed (+4) | Deployed |
| 2026-05-24 | `fd27b5e` | Admin console UI | New admin-only Flight Recorder tab inside existing `/app/admin` calls the protected `/api/admin/instagram/automation-flight-recorder?username=<name>&limit=200` endpoint via the existing authenticated API client. Account selector buttons (sourced from `multi-account-health`), Reload + Copy JSON. Summary panel shows latest comment_id_partial, whether `rule_loading_finished` / `rule_candidate_evaluated` fired for it, whether `automation_success` exists in the 200-event window, the `poller_account_scan_started` count, and a `suspected_candidate` classifier. Highlighted event rows for the 12 key stages. Same `canViewUsers` gate as Rule Coverage and Webhook Health. Diagnostics page route remains absent (`App.routes.test.js` still asserts it). Visibility only — no fix to Candidate 1 or 3 implemented here. | No backend or automation change | Frontend 184 passed; frontend production build passed; App.routes.test.js focused 2 passed | Deployed |
| 2026-05-24 | `0e5544b` | Admin webhook diagnostics | Record sanitized `change_fields` / `has_messaging` / `has_comment_field` in every `webhook_received` event so the operator can tell whether comment-field webhooks are actually arriving or only messaging-side ones. Surface a 50-sample aggregation as `webhook.recent_field_summary` in `/api/admin/instagram/multi-account-health` and the Webhook Health admin tab. Diagnostic only — no parser, resolver, rule-matching, send, HMAC, dedupe, or rate-limit change. | No automation behavior change | `py_compile backend/server.py`; backend 637 passed; frontend 184 passed; frontend build passed | Deployed |
| 2026-05-24 | `3b076f6` | Admin support reporting | Fix two reporting contradictions surfaced by Webhook Health: (1) `_latest_external_comment_event` now prefers the comment that produced an `automation_success` event, so a newer unrelated external comment (dedupe, sibling, activation cutoff) can no longer overwrite a verified success — Stop Point now correctly shows `rule_matched=true`, `reply_attempted=true`, `dm_attempted=true`, `exact_stop_reason=automation_success` for that comment; (2) new `_resolve_webhook_counters()` helper backs the top-level `webhook.last_received_at` / `webhook.last_processed_at` with the persistent flight recorder when the in-process globals are reset (Railway redeploy), so per-account `source=webhook` is no longer contradicted by a null top-level counter. No automation execution change, no rule-matching change, no send change. | No (reporting only) | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 104 passed (+4); backend 635 passed (+4) | Deployed |
| 2026-05-24 | `590e10a` | Admin console UI | Admin-only Webhook Health tab inside existing `/app/admin` renders the sanitized output of `/api/admin/instagram/multi-account-health`: top-level webhook last_received/last_processed and polling enabled/interval, plus per-account subscribed_fields, webhook_subscription_missing, last webhook/polling/comment/rule-match/automation-success times, stop_point_summary source/exact_stop_reason, and issues list. Visibility only; lets the operator classify the latency cause without an admin JWT. Same `canViewUsers` gate as Rule Coverage and Automation Stop Point. Diagnostics page route remains absent (`App.routes.test.js` still asserts it). | No backend or automation change | Frontend 184 passed; frontend production build passed | Deployed |
| 2026-05-24 | `61103fb` | Admin console UI | Admin-only Rule Coverage tab inside existing `/app/admin` renders the sanitized output of `/api/admin/instagram/rule-coverage-inspector`. Uses the existing authenticated API client; gated by the same `canViewUsers` check as Automation Stop Point; copies output as JSON. Diagnostics page route remains absent (`App.routes.test.js` still asserts it). | No backend or automation change | Frontend 184 passed; frontend production build passed | Deployed |
| 2026-05-24 | `72bc25c` | Admin support endpoint | New protected `/api/admin/instagram/rule-coverage-inspector` returns sanitized active rule shapes (raw + canonical-normalized trigger/scope + classification + should-match-latest-comment) per linked Instagram account. Diagnostic only — no automation logic change, no frontend page added, account-agnostic. | No | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 100 passed; backend 631 passed | Deployed, production build_sha `72bc25c1aa90` |
| 2026-05-24 | `54fb527` | Instagram rule matching | Legacy general any-post comment rules now match when top-level `trigger` is missing/`Manual` but `post_scope=any` or `nodes[].data.trigger=comment:any` identifies the rule as comment-capable. Account scoping, dedupe, HMAC, rate limits, and post-specific strictness are unchanged. | Yes | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 96 passed; backend 627 passed | Deployed via `a488bbb`; **live-verified on both linked Instagram accounts** in production (build_sha includes `61103fb1a270`); known-good for multi-account general any-post rule matching |
| 2026-05-24 | `e109f6e` | Instagram support diagnostics | Stop-point summary now prefers latest real external/commenter event over bot-owned public reply events, so `bot_own_reply` no longer hides the actual automation result. | No automation execution change | `py_compile backend/server.py`; `test_multi_account_automation_routing.py` 92 passed; backend 623 passed | Local, deploy required if accepted |
| 2026-05-24 | `d30f04e` | Instagram OAuth, frontend cache | Preserve existing IG connection on reused OAuth code; stop persisting failed media payloads; hide internal cache error from media picker. | Yes | Backend 622, frontend 184, build passed | Deployed, build_sha `d30f04e15582` |
| 2026-05-24 | `8ebe8c6` | OAuth, startup | Added OAuth code idempotency guard and moved heavy index/bootstrap work off startup healthcheck path. | Yes | Backend 621 reported in commit | Deployed before `d30f04e` |
| 2026-05-23 | `d547203` | Instagram token flow, media cache | Restored event-scoped token context for automation flow and refused to persist `ok=false` media snapshots. | Yes | Backend 614, frontend cache 17, build passed | Deployed before `8ebe8c6` |
| 2026-05-23 | `3500dcd` | Instagram token sends | Preferred stored account token before Graph sends. Later considered risky for Account 1 because stored account token can be stale. | Yes | Backend 614 | Deployed, superseded |
| 2026-05-23 | `5f5a34a` | Instagram token refresh | Added refresh retry after Graph code 190 for DM, public replies, and media listing. | Yes | Backend 612 | Deployed, superseded |
| 2026-05-23 | `582f644` | Comment-DM dedupe | Added completed-flow reopen TTL so old completed sessions do not block fresh live tests forever. | Yes | Backend 610 | Deployed |
| 2026-05-23 | `23e9f68` | Webhook security | Hardened webhook secret source resolution. | Yes | Focused webhook/security tests | Deployed |
| 2026-05-23 | `2141376` | Webhook security | Accepted Facebook app secret aliases for webhook verification. | Yes | Focused webhook tests | Deployed |
| 2026-05-23 | `129f420` | Quick reply fallback | Added automatic next-step DM delivery when click webhook is missing. | Yes | Backend focused tests | Deployed before later fixes |
| 2026-05-23 | `a770f7e` | Diagnostics | Classified post-opening-DM stop reasons deterministically. | Yes | Backend focused tests | Deployed before later fixes |
| 2026-05-23 | `868dc33` | Admin support | Added automation stop-point summary to protected admin console. | Yes | Backend/frontend focused tests | Deployed before diagnostics UI removal |
| 2026-05-23 | `deb5f21` | Support tooling | Added automation stop-point CLI for incident triage. | No user behavior | Backend focused tests | Local/support |
| 2026-05-23 | `c67dc96` | Support endpoint | Recorded automation stop points for comment flows. | Yes | Backend focused tests | Deployed |
| 2026-05-23 | `3dbef2c` | Quick replies | Continued after first quick reply for external users. | Yes | Backend focused tests | Deployed, later live issues remained |
| 2026-05-23 | `9e2363d` | External users | Continued comment-DM flows for external users. | Yes | Backend focused tests | Deployed, superseded |
| 2026-05-23 | `e2be355` | Rule matching | Ensured account-scoped and post-scoped automations share execution pipeline. | Yes | Backend focused tests | Deployed, superseded |
| 2026-05-23 | `0dfaa72` | Multi-account automation | Made comment automations fully multi-account instant. | Yes | Backend focused tests | Deployed, superseded |
| 2026-05-23 | `6e11993` | Frontend refactor | Centralized frontend routes and API clients. | No intended behavior change | Frontend tests/build | Deployed |
| 2026-05-23 | `d7b448b` | Backend refactor | Extracted Instagram account resolver. | No intended behavior change | Backend tests | Deployed |
| 2026-05-23 | `8fdc917` | Backend refactor | Extracted Instagram comment-DM flow service. | No intended behavior change | Backend tests | Deployed |
| 2026-05-23 | `422697b` | Backend refactor | Extracted Instagram rule normalizer. | No intended behavior change | Backend tests | Deployed |
| 2026-05-23 | `d29ec7d` | Frontend admin UI | Removed/hid diagnostics UI and added refactor plan. | Yes | Frontend tests/build | Deployed |
| 2026-05-23 | `7436984` | Multi-account routing | Isolated multi-account automation routing. | Yes | Backend focused tests | Deployed, superseded |
| 2026-05-23 | `f65cff7` | Idempotency | Enforced idempotent multi-account flow state. | Yes | Backend focused tests | Deployed, superseded |
