# Known Good Versions

Use this file before rollback or comparison work. A known-good version is not always perfect; it is a verified reference point.

| Commit SHA | What was working | What was not working | Why considered safe | Verification evidence | Rollback notes |
|---|---|---|---|---|---|
| `9b2af1bbd96e22d11bc373fed6430fcf11ee99ab` | **Dashboard UI polish only.** Dynamic chart title per selected range (`Performance — Last 24 hours` / `Last 7 days` / `Last 30 days` / `All time`); chart tooltip + axis labels render cleanly in the app UI language (forced `en` / `ar`) so an English UI never picks up Arabic month order from the browser locale; More stats toggle is a chip-style chevron button with i18n labels and focus styling; Total Contacts subtitle reads `All-time · Active account` (unambiguous next to range-scoped cards); Top Automations sorts active rules first, visually de-emphasizes paused/draft rows, capped at 3. All existing automation, dedupe, HMAC, webhook, polling, rate-limit, and Billing state from prior known-good `948a996` is preserved. | Meta webhook delivery for muhammad_gehad's comment-field events still unverified (polling fallback). Pending Instagram historical-cutoff fix from a separate session remains uncommitted in the working tree and is NOT covered by this known-good entry. Billing remains blocked. | This entry is scoped only to the dashboard frontend (`Dashboard.jsx` + `lib/dateTime.js` + `lib/i18n.js`). No backend file was modified between `948a996` and `9b2af1b` that touches automation, dedupe, webhook, polling, rule matching, send, HMAC, or rate-limit code paths. Dashboard polish is purely visual/labels. | Live operator confirmed the chart title now changes with range, tooltip reads cleanly in English (no Arabic month order), More stats is obviously clickable, Total Contacts subtitle is honest, and Top Automations shows active rules prominently. `/api/version` returned `9b2af1bbd96e`; `/api/` returned ok; frontend 205 passed; production build passed; `App.routes.test.js` still asserts `/app/admin/instagram-diagnostics` absent. | Safe rollback point for dashboard UI presentation. Does NOT change the automation pipeline relative to `948a996` — the underlying multi-account / polling-target / canonical-trigger logic is identical. |
| `948a9964e1d5b30fed432f71bc58cc167e76b75d` | Backend health; multi-account general automation across both linked Instagram accounts; legacy general any-post rule matching (`54fb527`) plus its mirror in polling target collection (`948a996`); polling target collection brings new posts into the polling scan for legacy general rules without `needs_any` set via top-level `trigger`; muhammad_gehad polling fallback successfully processes fresh new-post comments end-to-end; mogehad17 webhook success path unchanged; Stop Point / Rule Coverage / Webhook Health / Flight Recorder admin tabs all functional; diagnostics UI still absent. | Meta webhook delivery for muhammad_gehad's comment-field events is NOT verified — that account is still served by the 15-second polling fallback. The webhook delivery question (Advanced Access / App Review / Meta-side configuration) remains a separate open item and is explicitly NOT covered by this known-good entry. Billing remains blocked. | Polling target collection is now consistent with rule classification — both consult the canonical-trigger / canonical-post_scope helpers introduced by `54fb527`. Includes the silent-dedupe-early-return visibility patch (`d4dae1a`), the Flight Recorder admin tab (`fd27b5e`), the Webhook Health field-shape diagnostic (`0e5544b`), and the stop-point summarizer + persistent webhook-counter fixes (`3b076f6`). | `/api/version` returned `948a9964e1d5`; `/api/` returned ok; live retest from a completely new external Instagram account on a new post on muhammad_gehad produced reply + opening DM via polling fallback; mogehad17 fresh comment continued to succeed via webhook; backend 645 tests passed; multi-account 114 tests passed. | Safe rollback point for multi-account general automation, legacy general any-post rule matching, and polling target collection for new posts. Does NOT fix the underlying webhook-delivery gap on muhammad_gehad — rolling forward will still require Meta App Review / Advanced Access work to make webhook-first the primary path for that account. |
| `61103fb1a270d4b67e3d80b8d8fa24f139b7d885` | Backend health, multi-account automation parity, general any-post comment rule matching, Account 1 and Account 2 both firing automation (public reply + opening DM) on a single live retest, OAuth idempotency, non-blocking startup bootstrap, frontend cache safety, protected admin support endpoints including rule-coverage inspector and Automation Stop Point, diagnostics UI still absent. | Webhook delivery latency (`source=polling` on the stop-point summary) is NOT verified by this entry — comments still surfaced through the polling path. Meta Advanced Access / webhook subscription state is a separate open observation. Billing remains blocked. | Includes the live-verified `54fb527` legacy general-rule fix, the OAuth and bootstrap hardening from `8ebe8c6` + `d30f04e`, and the protected rule-coverage inspector. Both linked Instagram accounts produced reply + DM on a fresh live comment test. | `/api/version` returned `61103fb1a270`; `/api/` returned ok; Rule Coverage admin tab reported `should_match=true` for the active general rule on each linked account; the user confirmed automation fired on both accounts in a fresh live retest. | Safe rollback point for multi-account general automation. Does not include any fix for webhook delivery latency; rolling forward will still require Meta subscription / webhook investigation separately. |
| `d30f04e155828c61c85713142c10a6e7888a90d8` | Backend health, OAuth reused-code protection, frontend cache behavior for media failures, diagnostics UI still absent. | Instagram automation needs fresh live verification after deploy; Meta token issues may still require reconnect if Graph returns code 190. | Latest deployed commit with full backend/frontend tests passing and production build_sha verified. | `/api/version` returned `d30f04e15582`; `/api/` returned ok; backend 622 passed; frontend 184 passed; build passed. | Prefer forward fix unless this commit is directly tied to a regression. |
| `8ebe8c6b6b40b631bd4c1a7e0678589a4c5201a5` | Backend health, OAuth idempotency guard, nonblocking startup bootstrap. | Did not handle Meta returning "authorization code has been used" outside local consumed-code guard; media picker could show internal cache error after `ok=false`. | Deployed healthy state before `d30f04e`; no intended automation flow change. | `/api/version` previously returned `8ebe8c6b6b40`; backend tests reported 621 passed. | Safe comparison point if `d30f04e` suspected, but it has known OAuth/media UX gaps. |
| `d547203` | Restored event-scoped token context and media cache guard. | Did not include later OAuth idempotency and startup bootstrap hardening. | Important recovery point after risky stored-token preflight in `3500dcd`. | Backend 614 passed; frontend cache 17 passed; build passed. | Use as token-flow comparison baseline. |
| `23e9f68` | Webhook secret source resolution hardened and local dirty work had been reverted. | Later dedupe/token/media fixes absent. | Historical structural baseline after unsafe dirty diff was removed. | Production build_sha was verified at the time; backend health ok. | Use only if later Instagram flow changes need forensic comparison. |

## Rollback Reminder

Prefer `git revert <bad_commit>` over resetting history. Never rollback production without checking whether the target commit lacks security or data-integrity fixes.
